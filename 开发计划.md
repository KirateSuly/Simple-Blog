# 个人博客网站 · 页面设计与开发计划

> 技术栈：Next.js 15（App Router）+ React 19 + Tailwind CSS 4 + 极致毛玻璃
> 内容管理：Markdown + Git ｜ 音乐：本地音频 + 云存储 ｜ 部署：云服务器 + GitHub

---

## 一、项目概述

一个以「极致毛玻璃（Glassmorphism）」为视觉核心的个人博客，集齐：

- **首页**：个人介绍、轮播图、镶嵌式音乐播放器（含歌词）
- **项目 / 文章 / 说说 / 音乐 / 照片墙 / 杂谈 / 关于** 七大栏目
- 内容以 Markdown 文件管理，代码托管在 GitHub，静态资源（图片/音频/歌词）放云存储

---

## 二、技术选型确认

| 层 | 选型 | 说明 |
|---|---|---|
| 框架 | Next.js 15（App Router） | 默认服务端组件，按需客户端组件 |
| UI | React 19 | 用 `create-next-app@latest` 自带 React 19 |
| 样式 | Tailwind CSS 4 | CSS-first 配置（`@import "tailwindcss"` + `@theme`），无需 tailwind.config.js |
| 排版 | @tailwindcss/typography | 用 `@plugin` 引入，渲染 Markdown 文章正文 |
| 内容 | Markdown + Git | 文章/说说/杂谈以 `.mdx` 存仓库，前端解析 |
| MDX 解析 | gray-matter + next-mdx-remote/rsc | 解析 frontmatter 与渲染正文 |
| 代码高亮 | rehype-pretty-code + shiki | 文章内代码块高亮 |
| 音乐 | HTML5 `<audio>` + 自研封装 | 播放/暂停/上下首/音量/歌词同步 |
| 存储 | 云存储（OSS/COS/R2 任选）+ 本地 `/public` 兜底 | 图片、音频、歌词走对象存储 |
| 部署 | Docker + Nginx + GitHub Actions | 云服务器 |

---

## 三、目录结构（建议）

```
个人博客/
├─ app/                          # App Router 页面
│  ├─ layout.tsx                 # 根布局：Navbar + Footer + 全局播放器 Provider
│  ├─ page.tsx                   # 首页
│  ├─ globals.css                # Tailwind 4 入口 + 毛玻璃主题变量
│  ├─ projects/page.tsx          # 项目
│  ├─ articles/
│  │  ├─ page.tsx                # 文章列表（分页/标签）
│  │  └─ [slug]/page.tsx         # 文章详情
│  ├─ moments/page.tsx           # 说说时间线
│  ├─ music/page.tsx             # 音乐播放页
│  ├─ gallery/page.tsx           # 照片墙
│  ├─ essays/
│  │  ├─ page.tsx                # 杂谈列表
│  │  └─ [slug]/page.tsx         # 杂谈详情
│  └─ about/page.tsx             # 关于我自己
├─ components/
│  ├─ layout/Navbar.tsx          # 毛玻璃导航
│  ├─ layout/Footer.tsx
│  ├─ music/MusicPlayer.tsx      # 播放器核心（可嵌入 + 全屏）
│  ├─ music/MusicProvider.tsx    # 全局音乐状态 Context（跨页不中断）
│  ├─ music/useLrc.ts            # LRC 歌词解析 + 同步 Hook
│  ├─ home/Hero.tsx              # 个人介绍
│  ├─ home/Carousel.tsx          # 轮播图
│  ├─ gallery/PhotoWall.tsx      # 瀑布流
│  ├─ gallery/Lightbox.tsx       # 图片灯箱
│  ├─ ui/GlassCard.tsx           # 通用毛玻璃卡片
│  └─ mdx/Mdx.tsx                # MDX 渲染包装
├─ content/                      # 全部内容源（Git 管理）
│  ├─ articles/*.mdx             # 正式文章
│  ├─ essays/*.mdx               # 杂谈
│  ├─ moments/*.mdx              # 说说
│  ├─ projects/*.mdx             # 项目
│  └─ site.json                  # 站点全局配置（昵称/签名/社交/关于）
├─ lib/
│  ├─ content.ts                 # 统一读取+解析 MDX 的工具
│  └─ music.ts                   # 歌单数据（歌名/歌手/音频URL/lrc URL/封面）
├─ public/                       # 本地兜底资源（小图、默认封面）
├─ next.config.ts
├─ postcss.config.mjs
├─ Dockerfile
├─ .github/workflows/deploy.yml  # CI/CD
└─ package.json
```

---

## 四、全局设计规范

### 4.1 视觉基调：极致毛玻璃

- 背景：深色渐变 + 缓慢漂浮的光斑（blob），让玻璃有「背后有东西可透」的层次感
- 玻璃卡片统一配方（封装成 `GlassCard`）：

```css
backdrop-filter: blur(20px) saturate(160%);
background: rgba(255, 255, 255, 0.08);
border: 1px solid rgba(255, 255, 255, 0.15);
box-shadow: 0 8px 32px rgba(0, 0, 0, 0.35);
```

- 高光边：卡片顶部一条 `linear-gradient` 细线，模拟玻璃反光
- 噪点纹理：叠一层极淡 SVG noise，避免大面积渐变出现色带

### 4.2 Tailwind CSS 4 主题变量（`globals.css`）

```css
@import "tailwindcss";
@plugin "@tailwindcss/typography";

@theme {
  --color-accent: #7c6cff;          /* 主题强调色 */
  --font-sans: "Inter", "PingFang SC", "Microsoft YaHei", sans-serif;
  --glass-blur: 20px;
}

@custom-variant dark (&:where(.dark, .dark *));
```

### 4.3 动效

- 页面切换淡入 + 轻微上移（`framer-motion`，可选）
- 玻璃悬停：`hover:bg-white/15` + `hover:-translate-y-1` + 阴影加深
- 光斑用 CSS `@keyframes` 缓慢漂移，`prefers-reduced-motion` 时关闭

---

## 五、逐页设计

### 5.1 首页 `/`

自上而下三段式：

1. **Hero（个人介绍）**
   - 居中毛玻璃大卡片：头像（圆形玻璃环）、昵称、一句话签名、简短自我介绍
   - 社交图标（GitHub / 邮箱 / B站 等），hover 发光
   - 底部一个「滚动提示」小箭头
2. **轮播图（Carousel）**
   - 毛玻璃容器内自动轮播，左右箭头 + 底部圆点指示器
   - 每张卡片：背景图 + 标题 + 简介 + 可点击跳转
   - 触屏可滑动，自动播放 4s，hover 暂停
3. **镶嵌音乐播放器（Embedded MusicPlayer）**
   - 玻璃条卡片：封面（旋转黑胶动画）｜歌名/歌手｜播放/暂停 · 上一首 · 下一首｜音量滑杆｜进度条
   - **内嵌歌词区**：当前行高亮、已唱行变淡、自动滚动居中
   - 用全局 `MusicProvider` 驱动，**从首页跳到其它页面音乐不中断**
4. **内容速览**：最新 3 篇文章 + 最新几条说说（玻璃卡片网格）

### 5.2 项目 `/projects`

- 项目卡片网格（响应式 1/2/3 列）
- 每张卡片：封面图、项目名、简介、技术栈标签（毛玻璃 chip）、GitHub / 预览链接
- 点击进入项目详情（可复用文章详情渲染，或外链 GitHub）

### 5.3 文章 `/articles` + `/articles/[slug]`

- 列表页：玻璃卡片流，显示标题、摘要、日期、阅读时长、标签；支持按标签筛选 + 分页
- 详情页：
  - 顶部毛玻璃头图 + 标题 + 元信息
  - 正文用 `prose`（typography 插件）+ 毛玻璃侧边目录（TOC，桌面端固定）
  - 代码块高亮（shiki）+ 复制按钮
  - 底部「上一篇 / 下一篇」导航

### 5.4 说说 `/moments`

- 时间线（竖线 + 玻璃气泡）形式，非正式短内容
- 每条：内容、时间、可选配图、可选表情/心情标签
- 按时间倒序，支持「加载更多」

### 5.5 音乐 `/music`

- **完整播放页**：左侧歌单列表（玻璃面板），右侧大封面 + 完整歌词滚动
- 与首页嵌入播放器**共用同一套 `MusicProvider`**，切页状态同步
- 播放器控件：播放/暂停、上一首、下一首、音量、进度条拖拽、循环/随机模式（可选）
- 歌词：本地 `.lrc` 文件解析，逐行高亮同步

### 5.6 照片墙 `/gallery`

- 瀑布流（masonry）布局：CSS `columns` 或轻量库
- 图片用 `next/image`（需在 `next.config.ts` 配置远程域名）
- 点击放大：毛玻璃遮罩 + 灯箱（Lightbox），左右切换、ESC 关闭
- 可选按「相册/标签」分组

### 5.7 杂谈 `/essays` + `/essays/[slug]`

- 结构与文章一致，但定位更随意、短篇，UI 可更轻量（无 TOC 或简化）
- 可复用文章列表/详情的组件，仅数据源不同（`content/essays`）

### 5.8 关于 `/about`

- 详细自我介绍：照片、长文介绍、技能栈（进度条/标签云）、时间线（经历）、社交联系方式
- 数据可来自 `content/site.json` 或直接写死页面

---

## 六、数据模型

### 6.1 Frontmatter 统一 Schema

```yaml
---
title: 文章标题
description: 摘要
date: 2025-01-01
tags: [nextjs, react]
cover: /covers/xxx.jpg      # 封面（本地或云存储 URL）
draft: false                 # true 时不发布
---
正文（Markdown / MDX）
```

- 文章/杂谈额外字段：`readingTime`（可自动计算）、`toc`（是否显示目录）
- 说说额外字段：`mood`（心情）、`photos: [url]`
- 项目额外字段：`repo`、`demo`、`stack: [tech]`

### 6.2 站点全局配置 `content/site.json`

```json
{
  "name": "你的昵称",
  "signature": "一句话签名",
  "bio": "个人介绍",
  "avatar": "/avatar.jpg",
  "social": { "github": "...", "email": "...", "bilibili": "..." },
  "carousel": [{ "title": "...", "image": "...", "link": "..." }]
}
```

### 6.3 歌单数据 `lib/music.ts`

```ts
export const playlist = [
  {
    id: "1",
    title: "歌名",
    artist: "歌手",
    cover: "https://oss.../cover.jpg",
    src: "https://oss.../song.mp3",   // 云存储音频
    lrc: "https://oss.../song.lrc",   // 歌词文件
  },
];
```

---

## 七、核心组件技术方案

### 7.1 全局音乐播放器 `MusicProvider`（重点）

- 用 React Context 持有唯一 `<audio>` 实例（或用 `new Audio()`），页面切换不销毁
- 状态：`currentIndex` / `isPlaying` / `volume` / `currentTime` / `duration` / `lyrics`
- 对外方法：`play` / `pause` / `next` / `prev` / `setVolume` / `seek`
- `useEffect` 监听 `timeupdate` 事件，驱动歌词高亮
- 放在根 `layout.tsx`，首页嵌入组件和 `/music` 页共用

### 7.2 LRC 歌词解析 `useLrc`

- 解析 `[mm:ss.xx]` 行，转成 `{ time, text }[]`
- `timeupdate` 时用二分/线性找到当前行索引，高亮 + 滚动到可视区
- 处理多时间标签（同句重复）和无歌词的情况

### 7.3 轮播图 `Carousel`

- 纯 CSS/React 状态实现：`transform: translateX` + 过渡动画
- 自动播放用 `setInterval`，hover/触摸时暂停
- 指示器 + 左右箭头，无限循环（首尾克隆或取模）

### 7.4 照片墙 + 灯箱

- 瀑布流用 CSS `columns-*` 最轻量；如需精确排序再用 `masonry` 库
- 灯箱：`position: fixed` 全屏毛玻璃遮罩，图片居中，键盘事件（← → Esc）

---

## 八、分阶段开发计划

> 每个阶段可独立验证、可提交，建议按顺序推进。

| 阶段 | 内容 | 关键产出 | 预估 |
|---|---|---|---|
| **P0 初始化** | `create-next-app` + Tailwind 4 + 目录骨架 + 主题变量 + `GlassCard` | 能跑起来的空项目，毛玻璃底色 | 0.5 天 |
| **P1 布局与主题** | Navbar（毛玻璃/高亮当前路由/移动端菜单）、Footer、光斑背景、明暗主题 | 全局框架 + 导航完整 | 1 天 |
| **P2 首页** | Hero、Carousel、嵌入式播放器（含歌词）、内容速览 | 首页完整 | 2 天 |
| **P3 音乐系统** | `MusicProvider` + 完整播放器 + `/music` 页 + LRC 同步 | 音乐全局可用、跨页不中断 | 2 天 |
| **P4 内容系统** | `lib/content.ts` MDX 读取、`/articles` 列表 + 详情、`/essays` | 文章/杂谈可读、可发 | 2 天 |
| **P5 说说** | `/moments` 时间线 | 说说可发 | 1 天 |
| **P6 照片墙** | `/gallery` 瀑布流 + 灯箱 | 照片墙 | 1 天 |
| **P7 项目 & 关于** | `/projects`、`/about` | 两页完整 | 1 天 |
| **P8 打磨** | SEO（`generateMetadata`）、响应式、无障碍、性能、动效降级 | 上线质量达标 | 1 天 |
| **P9 部署** | Dockerfile、Nginx、GitHub Actions、云存储接入、环境变量 | 云服务器上线 | 1 天 |

**关键依赖清单**

```bash
npm install gray-matter next-mdx-remote rehype-pretty-code shiki @tailwindcss/typography
# 可选：framer-motion（动效）、lucide-react（图标）
```

---

## 九、部署方案

### 9.1 代码与资源

- 代码 + `content/` Markdown：GitHub 私有/公开仓库
- 图片、音频、歌词：云存储（阿里 OSS / 腾讯 COS / Cloudflare R2），用 `NEXT_PUBLIC_ASSET_URL` 环境变量切换
- 音频跨域：云存储需开启 CORS（`audio` 与 `fetch` 歌词需要）

### 9.2 服务器部署（推荐 Docker + Nginx）

1. 本地/CI 构建 `next build`（`output: "standalone"`）
2. Dockerfile 打包成镜像
3. 服务器：Nginx 反向代理到容器，配置 HTTPS（Let's Encrypt）
4. GitHub Actions：推送到 main 自动构建镜像 → 部署到服务器（SSH 或 Docker registry）

### 9.3 环境变量

```
NEXT_PUBLIC_ASSET_URL=https://你的云存储域名
NEXT_PUBLIC_SITE_URL=https://你的博客域名
```

---

## 十、注意事项与易踩坑

- **Next.js 15 异步参数**：动态路由里 `params`、`searchParams` 是 Promise，需 `await params`
- **Tailwind 4 配置方式已变**：无 `tailwind.config.js`，用 `@theme` + `@plugin`；别照搬 v3 教程
- **服务端读文件**：只在 Server Component / `lib/content.ts` 里用 `fs` + `process.cwd()` 读 `content/`
- **毛玻璃性能**：`backdrop-filter` 面积大时较吃性能，卡片数量多时慎用过大的 blur
- **云存储 CORS**：忘记配置会导致音乐播放/歌词拉取失败
- **`next/image` 远程图片**：云存储域名需加入 `next.config.ts` 的 `images.remotePatterns`

---

## 十一、下一步

确认本计划后，我可以从 **P0 初始化** 开始，逐步搭建代码。你可以：

1. 直接回复「开始搭建」，我从 P0 一路做到 P2（可运行的首页 + 音乐播放器）
2. 或先调整上面任意页面的设计（配色、布局、功能取舍）
